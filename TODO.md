# Dev-Sync — TODO

## Backend / auth

- [ ] JWT authentication (issue and validate tokens; replace placeholder `fake_decode_token` / OAuth2 flow).
- [ ] Password hash verification (use `verify_pwd` on login or token endpoint when credentials are checked).
